---
# defaults file for faasd

faasd_home: /srv/faasd
faasd_resolve_nameserver: 8.8.8.8
faasd_embed_caddy: yes
faasd_embed_containerd: yes
faasd_embed_cni_plugins: yes

faasd_cni_version: "0.8.5"
faasd_containerd_version: "1.5.4"

faasd_xcaddy_version: "0.1.9"
faasd_caddy_version: "2.4.3"
faasd_caddy_user: caddy
faasd_caddy_propagation: "/srv/caddy-loopia-propagation"
faasd_caddy_resolvers: "ns1.loopia.se"
faasd_caddy_api: no
faasd_caddy_apibase: http://localhost:2019/config

faasd_caddy_config: "{{ lookup('file', 'faasd.json') }}"